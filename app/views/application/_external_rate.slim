- object = ExternalRateDecorator.decorate external_rate

h2 Внешний курс (#{object.title})
- columns = %i(id snapshot created_at source sell_price buy_price)

= render 'attributes_table', object: object, columns: columns
